@model IEnumerable<WebThuCung.Models.Country>
@{
    ViewData["Title"] = "Danh sách quốc gia";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>Danh sách quốc gia</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Trang chủ</a></li>
                <li class="breadcrumb-item">Quốc gia</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Danh sách quốc gia</h5>

                        <div class="mb-3">
                            <a asp-action="Create" class="btn btn-primary">
                                <i class="bi bi-plus-circle"></i> Tạo quốc gia mới
                            </a>
                        </div>

                        <!-- Table with striped rows -->
                        <table class="table datatable">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tên quốc gia</th>
                                    <th>Sửa</th>
                                    <th>Xóa</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    foreach (var country in Model)
                                    {
                                        <tr>
                                            <td>@country.idCountry</td>
                                            <td>@country.nameCountry</td>
                                            <td>
                                                <!-- Edit Button -->
                                                <a asp-action="Edit" asp-route-id="@country.idCountry" class="btn btn-warning btn-sm">Sửa</a>
                                            </td>
                                            <td>
                                                <!-- Delete Button -->
                                                <form asp-action="Delete" asp-route-id="@country.idCountry" method="post" style="display:inline;">
                                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc chắn muốn xóa quốc gia này không?');">Xóa</button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="4" class="text-center">Không tìm thấy quốc gia nào.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Table with stripped rows -->

                    </div>
                </div>

            </div>
        </div>
    </section>

</main><!-- End #main -->
