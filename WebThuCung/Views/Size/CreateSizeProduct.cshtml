@model WebThuCung.Models.ProductSize

@{
    ViewData["Title"] = "Danh Sách Kích Thước";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<main id="main" class="main">
    <div class="pagetitle">
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Trang Chủ</a></li>
                <li class="breadcrumb-item"><a asp-controller="Size" asp-action="Index">Kích Thước</a></li>
                <li class="breadcrumb-item active">Tạo Mới</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-10">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Thêm Kích Thước Cho Sản Phẩm</h5>

                        <!-- Display validation summary -->
                        <div asp-validation-summary="All" class="text-danger"></div>

                        <!-- Create Product Size Form -->
                        <form asp-action="CreateSizeProduct" method="post">
                            <!-- Hidden field for Product ID -->
                            <input type="hidden" asp-for="idProduct" value="@ViewBag.ProductId" />

                            <!-- Size Selection -->
                            <div class="row mb-3">
                                <label asp-for="idSize" class="col-sm-2 col-form-label">Kích Thước</label>
                                <div class="col-sm-10">
                                    <select asp-for="idSize" class="form-control" required>
                                        <option value="">-- Chọn Kích Thước --</option>
                                        @foreach (var size in ViewBag.SizeList as List<SelectListItem>)
                                        {
                                            <option value="@size.Value">@size.Text</option>
                                        }
                                    </select>
                                </div>
                            </div>

                            <!-- Submit Button -->
                            <div class="row mb-3">
                                <div class="col-sm-10 offset-sm-2">
                                    <button type="submit" class="btn btn-primary">Lưu Thay Đổi</button>
                                    <a asp-action="SizeProduct" asp-route-id="@ViewBag.ProductId" class="btn btn-secondary">Quay Lại</a>
                                </div>
                            </div>
                            
                        </form><!-- End Create Product Size Form -->
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>
