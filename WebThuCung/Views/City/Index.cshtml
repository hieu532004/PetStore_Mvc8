@model IEnumerable<WebThuCung.Models.City>

@{
    ViewData["Title"] = "City List";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h1>Danh sách thành phố</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Trang chủ</a></li>
                <li class="breadcrumb-item">Chi nhánh</li>

            </ol>
        </nav>
    </div><!-- End Page Title -->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Danh sách thành phố</h5>

                        <div class="mb-3">
                            <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i>Tạo mới thành phố</a>
                        </div>
                        <table class="table datatable">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tên thành phố</th>
                                    <th>Quốc gia</th>
                                    <th>Hành động</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var city in Model)
                                {
                                    <tr>
                                        <td>@city.idCity</td>
                                        <td>@city.nameCity</td>
                                        <td>@city.Country?.nameCountry</td>
                                        <td>
                                            <a asp-action="Edit" asp-route-id="@city.idCity" class="btn btn-warning btn-sm">Sửa</a>
                                            <form asp-action="Delete" asp-route-id="@city.idCity" method="post" style="display:inline;">
                                                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc là bạn muốn xóa thành phố này?');">Xóa</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>
